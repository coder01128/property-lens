# Ticket 0001 — Setup Development Environment

## Goal
Get the project running locally with a standard React build setup so we can iterate safely and run tests.

## Acceptance Criteria
- ✅ The app builds and runs locally with a dev server.
- ✅ `inventory-app.jsx` renders and the UI is visible.
- ✅ A README section exists showing how to run the project.

## Tasks
- [ ] Initialize a React project (Vite is recommended).
- [ ] Add `inventory-app.jsx` to the new project (`src/inventory-app.jsx`).
- [ ] Create a simple `src/main.jsx` that renders the `App` component.
- [ ] Confirm `npm run dev` (or equivalent) starts a dev server and the app renders.

## Testing
- Start the dev server and verify the dashboard view appears (auth screen visible).
- Confirm no console errors on load.
